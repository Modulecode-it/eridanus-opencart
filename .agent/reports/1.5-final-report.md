**Задача:** 1.5-apiship-install
**Дата:** 2026-07-29
**Статус:** код подготовлен (ждёт ручных шагов в админке и подтверждения токена)

## Сводка

Подготовлена установка модуля доставки ApiShip 1.2 (OpenCart 3.x) в проект eridanus-opencart. Модуль совместим с OpenCart 3.0.3.2, конфликтов OCMOD с существующими модификаторами нет, все якоря модификатора присутствуют в реальных файлах проекта. Код подготовлен в ветке `feat/apiship-install`: в корень репозитория добавлены zip-архив и распакованный исходник `apiship-1.2_3.x.ocmod/` (по образцу задачи 1.4 Measoft).

**Главный открытый вопрос — токен.** Заказчик предоставил OAuth-токень Яндекса `y0__***` (для прямого API `b2b.taxi.yandex.net`), а модулю ApiShip нужен **свой** токен из ЛК `apiship.ru`. Без ApiShip-токена модуль устанавливается, но расчёт и экспорт не работают. Требуется подтверждение у заказчика выбранного пути интеграции.

## Что сделано (в репозитории)

1. Ветка `feat/apiship-install` создана от актуального `main` (`29b36f9`).
2. В корень добавлены:
   - `apiship-1.2_3.x.ocmod.zip` (оригинальный архив для Extension Installer);
   - `apiship-1.2_3.x.ocmod/` — распакованный исходник (23 файла): `install.xml` + `upload/{admin,catalog,system}/...`.
3. Проведён полный анализ OCMOD `install.xml`:
   - 8 целевых файлов, 16 операций, **все с `error="skip"`**;
   - единственное пересечение с существующими OCMOD — `admin/view/template/sale/order_info.twig` (патчится и `tweak.ocmod.xml`), но **якоря разные** → конфликта нет;
   - все якоря присутствуют в реальных файлах проекта → модификатор применится полностью.
4. Создана документация задачи 1.5 (spec/analysis/implementation/report), обновлены `INDEX.md`, `PROJECT_STRUCTURE.md`, `CHANGELOG.md`.

## Изменённые файлы

- `apiship-1.2_3.x.ocmod.zip` (новый)
- `apiship-1.2_3.x.ocmod/**` (23 новых файла)
- `.agent/specs/1.5-apiship-install.md` (новый)
- `.agent/analysis/1.5-apiship-install-analysis.md` (новый)
- `.agent/implementation/1.5-implementation-log.md` (новый)
- `.agent/reports/1.5-final-report.md` (этот файл, новый)
- `.agent/INDEX.md` (обновлён)
- `.agent/PROJECT_STRUCTURE.md` (обновлён)
- `.agent/CHANGELOG.md` (обновлён)

Файлы модуля **не** раскладывались по `admin/`, `catalog/`, `system/` — осознанно, по проектному паттерну (задача 1.4 Measoft): модуль ставится через Extension Installer из zip.

## Проверка

**Выполнено:**
- ✅ Анализ совместимости версии: пакет `1.2 (OpenCart 3.x)` совместим с OpenCart 3.0.3.2.
- ✅ Зависимости: PHP `curl`+`json` (стандарт), Simple Checkout установлен (задача 1.3), `select2` в комплекте.
- ✅ Анализ OCMOD-конфликтов: пересечений по якорям с tweak/availpro/simple_twig_fix/__fix_theme_editor нет.
- ✅ Проверка наличия якорей в реальных файлах: все 12 уникальных якорей найдены.
- ✅ Все операции `error="skip"` → исключено падение сайта при любых раскладах темы.

**Не выполнено** (требует живой админки/сервера — по `STACK.md` не делается локально):
- ⚠️ Загрузка zip через Extension Installer, Modifications → Refresh.
- ⚠️ Активация метода доставки, ввод токена ApiShip и ключа Яндекс.Карт JS.
- ⚠️ Визуальная проверка расчёта в чекауте и кнопок ApiShip в карточке заказа.
- ⚠️ Настройка cron на сервере.
- ⚠️ PHP/XML lint: `php`/`xmllint` отсутствуют в окружении; вендорный код не редактировался, поэтому линт не требуется.

## Известные пробелы

1. **Токен (блокер).** `y0__***` — OAuth Яндекса; модулю нужен токен ApiShip. Требует решения заказчика:
   - **(a)** Заказчик регистрируется в ЛК `apiship.ru`, привязывает Яндекс Доставку, даёт токен ApiShip → текущий путь (задача 1.5) верен.
   - **(b)** Нужна прямая интеграция с Яндекс Доставкой через `y0__` (`b2b.taxi.yandex.net`) → другой модуль/разработка, задача 1.5 **не покрывает**.
2. **Возвраты на ПВЗ Планерная 47** в модуле как отдельное поле не настраиваются — настраиваются в ЛК ApiShip (правила возврата) и/или на стороне провайдера.
3. **Наложенный платёж** (`cash_on_delivery_payment_methods`) точно работает только при включённой загрузке доставок после оплат в Simple.

## Ручные шаги после деплоя на сервер

1. **Установить модуль:**
   - Extensions → Installer → Upload → выбрать `apiship-1.2_3.x.ocmod.zip` → Install.
   - Если ругается на FTP/fileowner — установить вручную: распаковать `upload/*` в корень сайта, `install.xml` переименовать в `system/apiship.ocmod.xml`.
2. **Применить модификатор:**
   - Extensions → Modifications → найти «ApiShip» → кнопка **Refresh** (правый верх).
   - Очистить кэш: Dashboard → шестерёнка в правом верхнем углу (Clear theme/JS/SASS cache).
3. **Включить и настроить:**
   - Extensions → Extensions → фильтр типа = **Shipping** → найти «Расчёт доставки ApiShip» → **Enable** → **Edit**.
   - Вкладка «Основные»: `shipping_apiship_token` (токен ApiShip), режим Normal/Debug, статус Enabled.
   - Вкладка «Адрес»: страна/регион/город/улица/дом/офис = `197373, СПб, Планерная 47`; контакты отправителя (ФИО, телефон, email, ИНН, организация).
   - Вкладка «Шаблоны»: при желании иконки/ссылки ПВЗ и курьера; галка «Все ПВЗ на одной карте».
   - Карта ПВЗ: `shipping_apiship_yandex_api_key` (ключ Яндекс.Карт JS API).
   - Габариты/вес по умолчанию (`parcel_*`, `package_weight`).
   - Гео-зона = Россия; `tax_class`, `sort_order`, `prefix`, `include_fees`.
   - Вкладка «Провайдеры»: для **«Яндекс Доставка»** отметить `pickup_type` (ПВЗ) и `courier_type` (курьер); при «Привоз на склад» вставить код точки приёма Планерная 47 (из справочника ПВЗ в ЛК ApiShip).
   - Вкладка «Статусы»: `export_status`, `cancel_export_status`, групповой экспорт, `mapping_status` (сопоставить ApiShip↔OpenCart + «Оповестить»), `paid_orders`.
   - Наложенный платёж: `cash_on_delivery_payment_methods` (только при включённой загрузке доставок после оплат в Simple).
4. **Cron** (на сервере, через `cron_key` из настроек модуля):
   - `export_cron_url` — групповой экспорт заказов (раз в час/день);
   - `import_cron_url` — подтягивание трек-статусов.
5. **Проверка:** оформить тестовый заказ с доставкой в ПВЗ и курьером; в карточке заказа нажать «Выгрузить в ApiShip» → убедиться, что в ЛК ApiShip появилось отправление.

## Следующие шаги

- [ ] Слить ветку `feat/apiship-install` в `main` (после проверки токена с заказчиком) и задеплоить.
- [ ] Уточнить у заказчика путь интеграции: токен ApiShip (вариант a) или прямая Яндекс-интеграция (вариант b).
- [ ] После деплоя — пройти ручные шаги 1–5 выше (требует доступа к админке/серверу).
- [ ] При выборе варианта (b) — завести новую задачу (1.6 или отдельный эпик) под прямую интеграцию с `b2b.taxi.yandex.net`.

## Ссылки на документы задачи

- `.agent/specs/1.5-apiship-install.md`
- `.agent/analysis/1.5-apiship-install-analysis.md`
- `.agent/implementation/1.5-implementation-log.md`
- `.agent/reports/1.5-final-report.md` (этот файл)
