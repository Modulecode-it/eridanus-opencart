**Задача:** 0.5-ai-browser-access
**Дата:** 2026-08-19
**Статус:** завершён

## Сводка

Браузерное взаимодействие AI-агента с dev-сайтом `eridanus.dev.modulecode.ru`
формально разрешено и описано в правилах проекта. SSH зафиксирован как
недоступный (на сервере другие сайты). Введена схема кредов: отдельный
AI-пользователь админки + файл `.secrets/dev-admin.md` вне git (репо пушится на
GitHub, поэтому пароль в коммит не попадает). Введено обязательное правило:
перед важными/разрушающими операциями предлагать бекап. Учтено, что база —
копия боевой (реальные данные не трогать без одобрения).

## Изменённые Файлы

- `AGENTS.md` — секция «Главное правило» переизложена.
- `.agents/STACK.md` — «Проверка», «Деплой На Сервер», новая секция
  «Браузерный Доступ к Dev-Сайту (AI)».
- `.agents/skills/no-local-testing/SKILL.md` — переписан.
- `.agents/CHECKLISTS.md` — п.4.
- `.gitignore` — `.secrets/`.
- `.agents/templates/dev-secrets.md` — новый шаблон.
- `.secrets/dev-admin.md` — новая заготовка (вне git).
- Документы 0.5 + `INDEX.md` + `CHANGELOG.md`.

## Проверка

- ✅ Правила синхронны между `AGENTS.md`, `STACK.md`, `CHECKLISTS.md`, скиллом —
  противоречий (старого запрета) не осталось.
- ✅ `.gitignore` покрывает `.secrets/` (проверено до создания файла с кредами).
- ✅ Код не менялся; lint/runtime-проверки не требуются.
- ⚠️ Первый реальный заход браузером на dev-сайт ещё не выполнялся — станет
  возможен после заведения AI-пользователя и заполнения кредов.

## Известные Пробелы

1. AI-пользователь админки ещё не создан (шаг владельца).
2. Если dev-сайт когда-нибудь станет продакшеном — правила 0.5 откатить и
   отозвать AI-пользователя (зафиксировано в `dev-secrets.md`).
3. Внешние кабинеты (ЛК apiship.ru, Яндекс) — вне автоматизации; секреты
   владельца вводит сам.

## Следующие Шаги

- [ ] Владелец: System → Users → Users → Add New (`ai_agent`, Administrator,
      Enabled) → заполнить `.secrets/dev-admin.md`.
- [ ] Агент: проверить вход в админку через browser-use (первая сессия).
- [ ] Применить новый доступ: задача 1.5 шаги B5–B6 (Modifications → Refresh,
      включение и настройка ApiShip) — кандидаты на первую браузерную задачу.
