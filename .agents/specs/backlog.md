# Backlog

Будущие задачи и идеи для развития проекта.

Формат записей:

```markdown
- **<предполагаемый task-id>** — <краткое описание>. <почему важно>. <любые заметки>.
```

Когда задача берётся в работу — переносится в `specs/<task-id>-<slug>.md` и `INDEX.md`.

---

## Идеи из задач

- **1.7 (предложено)** — Отключить модуль MeaSoft (задача 1.4): клиент
  подтвердил (2026-08-19), что с Е-Логистик больше не работают; модуль включён
  с тестовым аккаунтом. Решить: отключить метод доставки на dev/бою или
  оставить до прихода новой ТК. Мелкая задача: admin → Shipping →
  «Основные настройки интеграции MeaSoft» + «Настройки доставки курьером» →
  Отключено.

## Вопросы к заказчику

Сводка открытых вопросов по выполненным задачам (собрана 2026-09-01 из
финальных отчётов 1.2–1.9 и журнала 1.9). При получении ответа — вычёркивать
пункт и заводить задачу/правку.

### Ждём решений

- **Наложенный платёж Яндекс.Доставки (1.7, находка 1).** Без доработки модуля
  не работает: экспресс-тарифы запрещают наложку, на курьерских ApiShip
  подставляет `paymentMethod='card'` (курьер принимает только наличные),
  модуль способ оплаты не передаёт. Сейчас включена обходная схема
  предоплаты. Выбор: предоплата / наложка только в ПВЗ / доработка модуля.
  Решение также определяет настройку 5Пост.
- **Реквизиты отправителя для накладных (1.7, открытый пункт 2).** Нужны
  реальные ФИО ответственного и email — сейчас заглушки («Команда
  интернет-магазина Eridanus» / ai_agent@local.com).
- **Судьба MeaSoft и старых методов (1.4 + 1.7 находка 4).** Пришли ли данные
  новой ТК (заказчик обещал «когда будут готовы»); отключить ли до того
  времени методы `measoftcourier` (×2) и `flat`, которые всё ещё активны
  в чекауте.
- **5Пост: активация тарифов (1.9, в работе).** Подключение выполнено
  полностью (connection 39490, склад, провайдер в модуле), но тарифы x5
  в калькуляторе ApiShip пустые — методы чекаута не появятся до активации
  тарифной сетки на стороне 5Пост/ApiShip. Уточнить у менеджера 5Пост
  статус активации; если за ~1 рабочий день тарифов нет — писать в
  поддержку ApiShip от лица клиента (внешний шаг, нужно одобрение
  владельца). Заодно: как физически сдаётся груз 5Пост со склада Планерная
  47 к 1.

### Организационные и мелкие

- **Тестовое отправление 40274852** в ЛК ApiShip (заказ #6713, «Новая
  заявка») — оставить владельцу для просмотра или отменить (1.7, пункт 4).
- **Ключ Яндекс.Карт JS API** — карта ПВЗ работает без него; нужен ли
  собственный ключ (1.7, пункт 5).
- **Вес товаров** — выгрузка в ApiShip требует вес заказа (целые граммы),
  пустое поле — ошибка валидации, сейчас вес вписывается вручную в карточку
  заказа. Будут ли заполнять вес/габариты в каталоге или настроить вес
  по умолчанию в модуле (1.7, находка 2).
- **Приёмка dev и перенос на боевой** — сроки; после приёмки отдельная
  задача: перенос токенов/настроек на боевой + повторить правку 1.8 (снять
  «Забирает курьер», точка приёма Планерная 47) (1.7, пункт 7; 1.8).
- **Twig-фиксы Simple** — нужен ли неустановленный
  `simple_twig_fix.ocmod.xml` из `system/` (1.2, известные пробелы).

> Не заказчику, а пользователю (серверный шаг): cron `export_orders` /
> `import_orders` по URL-ам из настроек модуля — без него статусы заказов
  не подтягиваются автоматически (1.7, пункт 1).

