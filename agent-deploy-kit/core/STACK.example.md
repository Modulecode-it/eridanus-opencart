# Стек: <НАЗВАНИЕ СТЕКА>

Последнее обновление: YYYY-MM-DD

> Этот файл — **единственный** в ядре, который меняется при смене стека. Всё остальное (`CORE.md`, `RULES.md`, `CHECKLISTS.md`, шаблоны) — универсально.
>
> Готовые варианты лежат в `agent-deploy-kit/stacks/` (`opencart.md`, `laravel.md`). Скопируй нужный сюда как `STACK.md` и адаптируй под проект.

## Стек и Версия

- Язык: <например PHP 8.2>
- Фреймворк: <например Laravel 11>
- Пакетный менеджер: <например composer, npm>
- БД: <например MySQL 8.0>
- Прочее: <например Redis, Horizon, Inertia>

## Структура Исходников

Краткая карта ключевых директорий и их роли. Полная карта — в `PROJECT_STRUCTURE.md`.

```text
<здесь — дерево ключевых исходных папок>
```

## Команды

| Действие | Команда |
|---|---|
| Поставить зависимости | `<команда>` |
| Линт / тайп-чек | `<команда>` |
| Тесты | `<команда>` |
| Миграции | `<команда>` |
| Запуск локально | `<команда>` |

## Правила Изменений Стека

1. <например: предпочитать Eloquent raw SQL>.
2. <например: миграции должны быть обратимыми (up + down)>.
3. <например: валидация — через Form Requests>.
4. <например: бизнес-логика — в Service/Action классах, не в контроллерах>.
5. <например: не редактировать сгенерированные файлы в `vendor/` / `storage/`>.

## Проверка

1. Линт/тайп-чек: `<команда>` для изменённых файлов.
2. Тесты: `<команда>` для затронутой области.
3. <стек-специфичная проверка — например, `php artisan route:list` после изменений маршрутов>.

## Пути, По Умолчанию Не Сканировать

- `vendor/`
- `node_modules/`
- `storage/`
- `bootstrap/cache/`
- `.git/`
- <другие большие/сгенерированные папки>

## Критичные Файлы И Секреты

- `.env` — содержит креды; **никогда** не копировать дословно в документы.
- <другие чувствительные файлы/пути проекта>.
